<% var majorVersions =  ['5', '6', '7', '8'] %>
<% var selectedMajorVersion = settings.jdkMajorVersion || '7'; %>
<!doctype html>
<html>
  <head>
    <title>JDK Inline Docs Settings</title>
  </head>
  <body>
    <form class="form-horizontal" >
      <div class="control-group">
        <label for="version_input" class="control-label">JDK Version</label>

        <div class="controls">
          <select name="jdkMajorVersion">
            <% for (var i = 0; i < majorVersions.length; i++) { %>
                <option
                <% if (selectedMajorVersion === majorVersions[i]) { %>
                     selected="selected"
                <% } %>
                >
                <%= majorVersions[i] %>
                </option>
            <% } %>
          </select>
        </div>
      </div>
      <div class="control-group">
        <div class="controls">
          <button type="submit" class="btn btn-primary">Save</button>
          <button type="button" class="btn">Cancel</button>
        </div>
      </div>
    </form>
  </body>
</html>
